# CS2Panel Hypervisor - Руководство по установке ISO

## 📀 О ISO образе

CS2Panel Hypervisor ISO - это полноценный загрузочный образ операционной системы (похож на VMware ESXi), который позволяет:

- Загружаться с USB/CD/DVD
- Работать в Live режиме без установки
- Устанавливать систему на физический сервер
- Автоматически настраивать гипервизор

## 🔧 Создание ISO образа

### Требования

**Операционная система:** Ubuntu/Debian Linux

**Необходимые пакеты:**
```bash
sudo apt-get update
sudo apt-get install -y \
    debootstrap \
    genisoimage \
    xorriso \
    isolinux \
    syslinux \
    squashfs-tools \
    grub-pc-bin \
    grub-efi-amd64-bin
```

**Свободное место на диске:** минимум 10 GB

### Сборка ISO

1. **Переход в директорию проекта:**
```bash
cd cs2panel
```

2. **Запуск сборки:**
```bash
sudo ./scripts/build-iso.sh
```

3. **Процесс сборки (20-40 минут):**
   - Создание базовой системы Ubuntu
   - Установка пакетов (QEMU, KVM, libvirt)
   - Настройка системы
   - Установка CS2Panel Hypervisor Daemon
   - Создание загрузочного образа

4. **Результат:**
```
📀 Файл: cs2panel-hypervisor-1.0.0.iso
📦 Размер: ~2-3 GB
```

## 💾 Запись ISO на носитель

### USB флешка (Linux)

**Внимание:** Все данные на USB будут удалены!

1. **Определите имя устройства:**
```bash
lsblk
# Найдите вашу флешку, например /dev/sdb
```

2. **Запись образа:**
```bash
sudo dd if=cs2panel-hypervisor-1.0.0.iso of=/dev/sdX bs=4M status=progress
sudo sync
```

3. **Безопасное извлечение:**
```bash
sudo eject /dev/sdX
```

### USB флешка (Windows)

**Используйте одну из программ:**

1. **Rufus (рекомендуется):**
   - Скачайте: https://rufus.ie/
   - Выберите ISO образ
   - Выберите USB устройство
   - Режим: DD Image
   - Нажмите START

2. **Etcher:**
   - Скачайте: https://www.balena.io/etcher/
   - Select image → выберите ISO
   - Select drive → выберите USB
   - Flash!

### USB флешка (macOS)

```bash
# Определите устройство
diskutil list

# Размонтируйте
diskutil unmountDisk /dev/diskX

# Запишите образ
sudo dd if=cs2panel-hypervisor-1.0.0.iso of=/dev/rdiskX bs=4m

# Извлеките
diskutil eject /dev/diskX
```

### DVD диск

Используйте любую программу для записи ISO:
- Windows: ImgBurn, CDBurnerXP
- Linux: Brasero, K3b
- macOS: Disk Utility

## 🖥️ Установка на сервер

### Требования к серверу

**Минимальные требования:**
- CPU: 4 ядра (с поддержкой виртуализации VT-x/AMD-V)
- RAM: 8 GB
- Disk: 100 GB
- Network: 1 Gbit/s

**Рекомендуемые требования:**
- CPU: 16+ ядер
- RAM: 64+ GB
- Disk: 1 TB SSD (NVMe предпочтительнее)
- Network: 10 Gbit/s

**BIOS настройки:**
- Включите виртуализацию (VT-x/AMD-V)
- Включите VT-d/IOMMU (для PCI passthrough)
- UEFI режим (рекомендуется)

### Процесс установки

#### 1. Загрузка с USB/DVD

1. Вставьте USB флешку или DVD
2. Перезагрузите сервер
3. Войдите в Boot Menu (обычно F12, F11, или ESC)
4. Выберите USB/DVD устройство

#### 2. Меню загрузки

После загрузки появится меню:

```
╔═══════════════════════════════════════════════════╗
║      CS2Panel Hypervisor Installer                ║
╚═══════════════════════════════════════════════════╝

1. Install CS2Panel Hypervisor (рекомендуется)
2. Live Mode (без установки)
3. Rescue Mode
```

**Выберите опцию 1: Install CS2Panel Hypervisor**

#### 3. Мастер установки

**Шаг 1: Приветствие**
```
╔═══════════════════════════════════════════════════╗
║   CS2Panel Hypervisor Installation Wizard        ║
║              Version 1.0.0                        ║
╚═══════════════════════════════════════════════════╝

✅ Режим загрузки: UEFI
```

**Шаг 2: Выбор диска**
```
Доступные диски:
1. sda    500GB    disk
2. sdb    1TB      disk

Выберите диск для установки (например, sda): sda
```

**Шаг 3: Подтверждение**
```
⚠️  ВНИМАНИЕ: ВСЕ ДАННЫЕ НА /dev/sda (500GB) БУДУТ УДАЛЕНЫ!
Продолжить? (yes/no): yes
```

**Шаг 4: Установка (5-15 минут)**
```
🔧 Начало установки...
🧹 Очистка диска...
📁 Создание разделов...
💾 Форматирование разделов...
📦 Копирование системы...
🔧 Установка загрузчика GRUB...
🌐 Настройка сети...
```

**Шаг 5: Настройка hostname**
```
Введите hostname (по умолчанию cs2panel-hypervisor): my-hypervisor
```

**Шаг 6: Завершение**
```
╔═══════════════════════════════════════════════════╗
║   ✅ Установка завершена успешно!                 ║
╚═══════════════════════════════════════════════════╝

Система установлена на /dev/sda

После перезагрузки:
  • Логин: cs2admin
  • Пароль: cs2panel
  • API: http://192.168.1.100:8080

Нажмите Enter для перезагрузки...
```

#### 4. Первый запуск

После перезагрузки:

1. Извлеките USB/DVD
2. Система загрузится автоматически
3. Появится экран входа:

```
╔═══════════════════════════════════════════════════╗
║          CS2Panel Hypervisor - Login              ║
╚═══════════════════════════════════════════════════╝

cs2panel-hypervisor login: cs2admin
Password: cs2panel
```

4. После входа появится приветственное сообщение:

```
╔═══════════════════════════════════════════════════╗
║              CS2Panel Hypervisor                  ║
║                  Version 1.0.0                    ║
╚═══════════════════════════════════════════════════╝

🔧 Управление:
   • Статус: systemctl status cs2panel-hypervisor
   • Логи:   journalctl -u cs2panel-hypervisor -f
   • API:    curl http://localhost:8080/v1/health
```

## 🔧 Первоначальная настройка

### 1. Изменение пароля (обязательно!)

```bash
passwd
```

### 2. Настройка сети

**Статический IP:**
```bash
sudo nano /etc/netplan/00-installer-config.yaml
```

Содержимое:
```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    ens192:  # Ваш интерфейс
      addresses:
        - 192.168.1.100/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

Применить:
```bash
sudo netplan apply
```

### 3. Проверка сервисов

```bash
# Статус гипервизора
systemctl status cs2panel-hypervisor

# Статус libvirt (KVM)
systemctl status libvirtd

# Проверка виртуализации
virt-host-validate
```

### 4. Проверка API

```bash
curl http://localhost:8080/v1/health
```

Ответ:
```json
{
  "status": "healthy",
  "service": "hypervisor-daemon"
}
```

### 5. Создание SSH ключа

```bash
ssh-keygen -t ed25519 -C "cs2admin@cs2panel"
```

### 6. Настройка firewall (опционально)

```bash
# Установка ufw
sudo apt install ufw

# Разрешить SSH
sudo ufw allow 22/tcp

# Разрешить API
sudo ufw allow 8080/tcp

# Разрешить gRPC
sudo ufw allow 9090/tcp

# Включить firewall
sudo ufw enable
```

## 🧪 Тестирование

### Создание тестовой VM

```bash
curl -X POST http://localhost:8080/v1/vms \
  -H "Content-Type: application/json" \
  -d '{
    "name": "test-vm",
    "cpu_cores": 2,
    "memory_mb": 2048,
    "disk_gb": 20,
    "os_type": "linux"
  }'
```

### Просмотр списка VM

```bash
curl http://localhost:8080/v1/vms
```

### Просмотр метрик

```bash
curl http://localhost:8080/v1/metrics
```

## 🔗 Подключение к панели управления

После установки гипервизор готов к подключению к центральной панели управления CS2Panel:

1. В панели управления перейдите: **Nodes** → **Add Node**

2. Заполните данные:
   - **Hostname:** my-hypervisor
   - **IP Address:** 192.168.1.100
   - **Port:** 8080
   - **gRPC Port:** 9090

3. Нажмите **Test Connection**

4. После успешного подключения нода появится в списке

## 🛠️ Обслуживание

### Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### Обновление hypervisor daemon

1. Скомпилируйте новую версию
2. Скопируйте на сервер:
```bash
scp hypervisor-daemon cs2admin@192.168.1.100:/tmp/
```

3. Замените и перезапустите:
```bash
sudo systemctl stop cs2panel-hypervisor
sudo mv /tmp/hypervisor-daemon /usr/local/bin/
sudo chmod +x /usr/local/bin/hypervisor-daemon
sudo systemctl start cs2panel-hypervisor
```

### Просмотр логов

```bash
# Логи hypervisor
journalctl -u cs2panel-hypervisor -f

# Логи libvirt
journalctl -u libvirtd -f

# Системные логи
journalctl -f
```

### Резервное копирование

```bash
# Бэкап конфигурации
sudo tar -czf cs2panel-backup-$(date +%Y%m%d).tar.gz \
    /etc/systemd/system/cs2panel-hypervisor.service \
    /var/lib/cs2panel/ \
    /etc/netplan/

# Бэкап VM дисков
sudo virsh list --all
sudo cp /var/lib/libvirt/images/vm-disk.qcow2 /backup/
```

## ❓ Устранение неполадок

### ISO не загружается

**Проблема:** Система не загружается с USB

**Решение:**
- Проверьте BIOS/UEFI настройки
- Убедитесь, что Secure Boot отключен
- Попробуйте другой USB порт (USB 2.0)
- Перезапишите ISO другой программой

### Нет сетевого подключения

**Проблема:** Нет доступа к сети после установки

**Решение:**
```bash
# Проверьте интерфейсы
ip link show

# Проверьте netplan
sudo netplan --debug apply

# Перезапустите сеть
sudo systemctl restart systemd-networkd
```

### Hypervisor daemon не запускается

**Проблема:** Сервис не стартует

**Решение:**
```bash
# Проверьте статус
systemctl status cs2panel-hypervisor

# Проверьте логи
journalctl -u cs2panel-hypervisor -n 50

# Проверьте права
ls -la /usr/local/bin/hypervisor-daemon

# Запустите вручную
/usr/local/bin/hypervisor-daemon
```

### KVM не работает

**Проблема:** Виртуализация недоступна

**Решение:**
```bash
# Проверьте поддержку виртуализации
egrep -c '(vmx|svm)' /proc/cpuinfo
# Должно быть > 0

# Проверьте модули
lsmod | grep kvm

# Загрузите модули
sudo modprobe kvm
sudo modprobe kvm_intel  # или kvm_amd

# Проверьте валидацию
virt-host-validate
```

## 📚 Дополнительные ресурсы

- **Документация:** https://docs.cs2panel.example.com
- **GitHub:** https://github.com/cs2panel/cs2panel
- **Поддержка:** support@cs2panel.example.com
- **Discord:** https://discord.gg/cs2panel

## 📝 Лицензия

MIT License - см. LICENSE файл

---

**Готово! Ваш CS2Panel Hypervisor установлен и готов к работе! 🎉**
