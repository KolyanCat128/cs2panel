# Установка CS2Panel Hypervisor на Ubuntu 24.04

## Быстрый старт

Этот скрипт устанавливает CS2Panel Hypervisor на уже установленную Ubuntu 24.04 (в отличие от ISO установщика).

### Требования

- ✅ Ubuntu 24.04 LTS (или новее)
- ✅ Минимум 4GB RAM (рекомендуется 8GB+)
- ✅ Минимум 10GB свободного места
- ✅ Процессор с поддержкой виртуализации (Intel VT-x / AMD-V)
- ✅ Права root/sudo

### Установка

#### Шаг 1: Скачайте проект

Если вы на Windows, скопируйте скрипт на вашу Ubuntu VM:

```bash
# На Ubuntu VM
# Создайте директорию
mkdir -p ~/cs2panel
cd ~/cs2panel

# Скопируйте скрипт (например, через SSH/SCP из Windows)
# Или создайте файл вручную
nano install-hypervisor.sh
# Вставьте содержимое скрипта и сохраните (Ctrl+O, Enter, Ctrl+X)
```

#### Шаг 2: Сделайте скрипт исполняемым

```bash
chmod +x install-hypervisor.sh
```

#### Шаг 3: Запустите установку

```bash
sudo ./install-hypervisor.sh
```

Скрипт выполнит:

1. ✅ Проверку системы (Ubuntu 24.04, виртуализация, диски)
2. ✅ Обновление системы
3. ✅ Установку QEMU/KVM и libvirt
4. ✅ Установку системных утилит
5. ✅ Создание пользователя cs2admin
6. ✅ Установку CS2Panel Hypervisor Daemon
7. ✅ Настройку systemd сервисов
8. ✅ Настройку SSH
9. ✅ Оптимизацию для виртуализации
10. ✅ Оптимизацию для NVMe (если обнаружен)

### Время установки

| Диск     | Время        |
|----------|--------------|
| NVMe     | 10-15 минут  |
| SSD      | 15-20 минут  |
| HDD      | 20-30 минут  |

### После установки

#### 1. Перезагрузите систему

```bash
sudo reboot
```

#### 2. Проверьте статус

```bash
# Проверить CS2Panel Hypervisor
systemctl status cs2panel-hypervisor

# Проверить libvirt
systemctl status libvirtd

# Проверить API
curl http://localhost:8080/v1/health
```

#### 3. Войдите под пользователем cs2admin

```bash
# Локально
su - cs2admin

# Или по SSH
ssh cs2admin@<your-ip>
```

**Учетные данные по умолчанию:**
- Пользователь: `cs2admin`
- Пароль: `cs2panel`

**⚠️ ОБЯЗАТЕЛЬНО СМЕНИТЕ ПАРОЛЬ:**

```bash
passwd
```

### Настройка сети (если не работает)

Если после установки у вас проблемы с сетью:

#### Для VMware Workstation

##### Вариант 1: NAT (самый простой)

```bash
# В VMware: VM → Settings → Network Adapter → NAT
# Перезагрузите VM
sudo reboot
```

##### Вариант 2: Bridged с настройкой

```bash
# Проверить интерфейсы
ip addr

# Настроить Netplan
sudo nano /etc/netplan/01-netcfg.yaml
```

Для DHCP:
```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:  # ваш интерфейс (может быть другой)
      dhcp4: true
      dhcp6: false
```

Для статического IP:
```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:
      dhcp4: false
      addresses:
        - 192.168.1.100/24
      routes:
        - to: default
          via: 192.168.1.1
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]
```

Применить настройки:
```bash
sudo netplan apply
```

### Управление сервисами

```bash
# Статус
systemctl status cs2panel-hypervisor

# Запуск
sudo systemctl start cs2panel-hypervisor

# Остановка
sudo systemctl stop cs2panel-hypervisor

# Перезапуск
sudo systemctl restart cs2panel-hypervisor

# Логи
journalctl -u cs2panel-hypervisor -f

# Последние 100 строк
journalctl -u cs2panel-hypervisor -n 100
```

### Работа с виртуальными машинами

```bash
# Список всех VM
virsh list --all

# Информация о VM
virsh dominfo <vm-name>

# Запуск VM
virsh start <vm-name>

# Остановка VM
virsh shutdown <vm-name>

# Принудительная остановка
virsh destroy <vm-name>

# Подключение к консоли VM
virsh console <vm-name>

# Выход из консоли: Ctrl+]
```

### Проверка производительности

```bash
# Проверить CPU
lscpu

# Проверить RAM
free -h

# Проверить диски
df -h

# Проверить NVMe
nvme list

# Проверить виртуализацию
virt-host-validate

# Проверить KVM
kvm-ok
```

### Оптимизация NVMe

Скрипт автоматически оптимизирует NVMe диски. Проверить:

```bash
# Проверить scheduler
cat /sys/block/nvme0n1/queue/scheduler

# Должно быть: [none]

# Проверить nr_requests
cat /sys/block/nvme0n1/queue/nr_requests

# Должно быть: 256
```

### Компиляция реального Hypervisor Daemon

Если вы хотите заменить заглушку на реальный daemon:

```bash
# Установите Go (если еще не установлен)
sudo apt-get install -y golang-go

# Перейдите в директорию проекта
cd ~/cs2panel/hypervisor

# Скомпилируйте
go build -o bin/hypervisor-daemon .

# Установите
sudo cp bin/hypervisor-daemon /usr/local/bin/

# Перезапустите сервис
sudo systemctl restart cs2panel-hypervisor

# Проверьте
systemctl status cs2panel-hypervisor
```

### Настройка Firewall (опционально)

```bash
# Установка UFW
sudo apt-get install -y ufw

# Разрешить SSH
sudo ufw allow 22/tcp

# Разрешить API
sudo ufw allow 8080/tcp

# Разрешить gRPC
sudo ufw allow 9090/tcp

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

### Устранение неполадок

#### CS2Panel не запускается

```bash
# Проверить логи
journalctl -u cs2panel-hypervisor -n 50

# Проверить конфигурацию
cat /etc/cs2panel/hypervisor.yaml

# Проверить права
ls -la /usr/local/bin/hypervisor-daemon

# Должно быть: -rwxr-xr-x root root
```

#### libvirt не работает

```bash
# Перезапустить libvirt
sudo systemctl restart libvirtd

# Проверить логи
journalctl -u libvirtd -n 50

# Проверить модуль KVM
lsmod | grep kvm

# Загрузить модуль если нужно
sudo modprobe kvm
sudo modprobe kvm_intel  # или kvm_amd
```

#### Нет доступа к виртуализации

```bash
# Проверить права пользователя
groups

# Должны быть: libvirt kvm

# Если нет - добавить
sudo usermod -aG libvirt,kvm $USER

# Выйти и войти снова
exit
```

#### Проблемы с сетью в VMware

1. **DHCP не работает в Bridged:**
   - Переключитесь на NAT
   - Или настройте статический IP

2. **Нет доступа к интернету:**
   ```bash
   # Проверить DNS
   cat /etc/resolv.conf

   # Проверить gateway
   ip route

   # Пинг Google
   ping -c 4 8.8.8.8
   ```

3. **VMware Tools:**
   ```bash
   # Установить open-vm-tools
   sudo apt-get install -y open-vm-tools
   ```

### Файлы и директории

- Конфигурация: `/etc/cs2panel/hypervisor.yaml`
- Daemon: `/usr/local/bin/hypervisor-daemon`
- Systemd сервис: `/etc/systemd/system/cs2panel-hypervisor.service`
- Данные: `/var/lib/cs2panel/`
- Логи: `/var/log/cs2panel/`
- Лог установки: `/var/lib/cs2panel/install.log`

### Удаление (если нужно)

```bash
# Остановить сервисы
sudo systemctl stop cs2panel-hypervisor
sudo systemctl disable cs2panel-hypervisor

# Удалить файлы
sudo rm /usr/local/bin/hypervisor-daemon
sudo rm /etc/systemd/system/cs2panel-hypervisor.service
sudo rm -rf /etc/cs2panel
sudo rm -rf /var/lib/cs2panel
sudo rm -rf /var/log/cs2panel

# Удалить пользователя (опционально)
sudo userdel -r cs2admin

# Удалить пакеты (опционально)
sudo apt-get remove --purge qemu-kvm libvirt-daemon-system
```

## Дополнительная информация

### Системные требования для production

| Компонент | Минимум | Рекомендуется | Оптимально |
|-----------|---------|---------------|------------|
| CPU       | 2 cores | 4 cores       | 8+ cores   |
| RAM       | 4 GB    | 8 GB          | 16+ GB     |
| Диск      | 20 GB   | 100 GB        | 500+ GB    |
| Сеть      | 100 Mbps| 1 Gbps        | 10 Gbps    |

### Поддерживаемые функции

- ✅ Создание и управление KVM виртуальными машинами
- ✅ REST API для управления
- ✅ gRPC API для высокопроизводительных операций
- ✅ Мониторинг ресурсов
- ✅ Управление сетями
- ✅ Управление storage pools
- ✅ Live migration (требует настройки)
- ✅ Snapshots и backups
- ✅ UEFI и Legacy BIOS загрузка
- ✅ NVMe оптимизация

### Следующие шаги

1. Настройте сеть в вашей VM
2. Скомпилируйте реальный hypervisor daemon
3. Создайте первую виртуальную машину
4. Настройте мониторинг
5. Настройте автоматические бэкапы

## Поддержка

- GitHub: https://github.com/cs2panel/hypervisor
- Документация: https://docs.cs2panel.example.com
- Issues: https://github.com/cs2panel/hypervisor/issues

---

**Приятной работы с CS2Panel! 🚀**
