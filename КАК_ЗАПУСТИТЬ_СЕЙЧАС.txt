═══════════════════════════════════════════════════════════════
  КАК ЗАПУСТИТЬ УСТАНОВКУ CS2PANEL ПРЯМО СЕЙЧАС
═══════════════════════════════════════════════════════════════

📍 ВЫ ЗДЕСЬ: Ubuntu 24.04 уже установлена в VMware Workstation

🎯 ЦЕЛЬ: Установить CS2Panel Hypervisor с оптимизацией для NVMe


═══════════════════════════════════════════════════════════════
  ВАРИАНТ 1: САМЫЙ ПРОСТОЙ (копировать-вставить в терминал)
═══════════════════════════════════════════════════════════════

Шаг 1: Откройте терминал в вашей Ubuntu VM

Шаг 2: Создайте скрипт установки одной командой:

Скопируйте ЭТУ команду в ваш терминал Ubuntu:
-------------------------------------------------------------------

cat > ~/install-hypervisor.sh << 'EOF'
[ЗДЕСЬ БУДЕТ ВСЁ СОДЕРЖИМОЕ СКРИПТА install-hypervisor.sh]
EOF

-------------------------------------------------------------------

ИЛИ используйте nano (проще для копирования большого текста):

1. В терминале Ubuntu выполните:
   nano ~/install-hypervisor.sh

2. Откройте в Windows файл:
   C:\Users\Галина\12345\scripts\install-hypervisor.sh

3. Скопируйте ВСЁ содержимое (Ctrl+A, Ctrl+C)

4. В терминале Ubuntu вставьте (правая кнопка мыши или Shift+Insert)

5. Сохраните: Ctrl+O, Enter, Ctrl+X


Шаг 3: Сделайте скрипт исполняемым:

chmod +x ~/install-hypervisor.sh


Шаг 4: Запустите установку:

sudo ./install-hypervisor.sh


Шаг 5: Дождитесь завершения (10-30 минут)

Скрипт покажет:
  ✅ Проверки системы
  ✅ Установку пакетов
  ✅ Настройку сервисов
  ✅ Оптимизации для NVMe


Шаг 6: После завершения перезагрузите:

sudo reboot


Шаг 7: Проверьте работу:

systemctl status cs2panel-hypervisor
curl http://localhost:8080/v1/health
virsh list --all


═══════════════════════════════════════════════════════════════
  ВАРИАНТ 2: ЧЕРЕЗ SHARED FOLDER (если настроена в VMware)
═══════════════════════════════════════════════════════════════

1. В VMware Workstation:
   - VM → Settings → Options → Shared Folders
   - Enable: "Always enabled"
   - Add: C:\Users\Галина\12345

2. В Ubuntu VM:
   sudo apt-get install -y open-vm-tools
   sudo mkdir -p /mnt/hgfs
   sudo mount -t fuse.vmhgfs-fuse .host:/ /mnt/hgfs -o allow_other

3. Скопируйте скрипт:
   cp /mnt/hgfs/12345/scripts/install-hypervisor.sh ~/
   chmod +x ~/install-hypervisor.sh
   sudo ./install-hypervisor.sh


═══════════════════════════════════════════════════════════════
  ЕСЛИ СЕТЬ НЕ РАБОТАЕТ (нет DHCP в установщике)
═══════════════════════════════════════════════════════════════

РЕШЕНИЕ 1: Переключитесь на NAT (самое быстрое!)

1. Выключите VM:
   sudo poweroff

2. В VMware Workstation:
   - VM → Settings → Network Adapter
   - Выберите: "NAT: Used to share the host's IP address"
   - OK

3. Запустите VM

4. Проверьте сеть:
   ip addr
   # Должен появиться IP адрес (обычно 192.168.x.x)


РЕШЕНИЕ 2: Настройте статический IP в Bridged

1. Узнайте имя сетевого интерфейса:
   ip addr
   # Обычно: ens33, eth0, или enp0s3

2. Настройте Netplan:
   sudo nano /etc/netplan/01-netcfg.yaml

3. Для DHCP вставьте:
-------------------------------------------------------------------
network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:  # ЗАМЕНИТЕ на ваш интерфейс!
      dhcp4: true
      dhcp6: false
-------------------------------------------------------------------

4. Для статического IP вставьте:
-------------------------------------------------------------------
network:
  version: 2
  renderer: networkd
  ethernets:
    ens33:  # ЗАМЕНИТЕ на ваш интерфейс!
      dhcp4: false
      addresses:
        - 192.168.1.100/24  # ЗАМЕНИТЕ на ваш IP!
      routes:
        - to: default
          via: 192.168.1.1  # ЗАМЕНИТЕ на ваш gateway!
      nameservers:
        addresses: [8.8.8.8, 8.8.4.4]
-------------------------------------------------------------------

5. Примените настройки:
   sudo netplan apply

6. Проверьте:
   ip addr
   ping -c 4 8.8.8.8


═══════════════════════════════════════════════════════════════
  ПРОВЕРКА ПЕРЕД УСТАНОВКОЙ
═══════════════════════════════════════════════════════════════

Убедитесь что всё готово:

1. Проверьте версию Ubuntu:
   lsb_release -a
   # Должно быть: Ubuntu 24.04

2. Проверьте свободное место (минимум 10GB):
   df -h /
   # Смотрите столбец "Avail"

3. Проверьте RAM (минимум 4GB):
   free -h
   # Смотрите строку "Mem:" столбец "total"

4. Проверьте sudo:
   sudo -v
   # Введите пароль

5. Проверьте виртуализацию:
   grep -E 'vmx|svm' /proc/cpuinfo
   # Должно что-то вывести


Если ВСЁ ОК → запускайте установку!


═══════════════════════════════════════════════════════════════
  ЧТО ДЕЛАТЬ ПОСЛЕ УСТАНОВКИ
═══════════════════════════════════════════════════════════════

1. ОБЯЗАТЕЛЬНО СМЕНИТЕ ПАРОЛЬ:

   # Войдите под cs2admin
   su - cs2admin
   # Пароль: cs2panel

   # Смените пароль
   passwd
   # Введите новый пароль дважды


2. Проверьте сервисы:

   systemctl status cs2panel-hypervisor
   systemctl status libvirtd


3. Проверьте API:

   curl http://localhost:8080/v1/health


4. Проверьте виртуализацию:

   virsh list --all
   virt-host-validate


5. Проверьте NVMe оптимизации (если есть NVMe):

   cat /sys/block/nvme0n1/queue/scheduler
   # Вывод: [none]

   cat /sys/block/nvme0n1/queue/nr_requests
   # Вывод: 256


═══════════════════════════════════════════════════════════════
  УЧЕТНЫЕ ДАННЫЕ ПО УМОЛЧАНИЮ
═══════════════════════════════════════════════════════════════

Пользователь: cs2admin
Пароль:       cs2panel

API:          http://localhost:8080
gRPC:         localhost:9090


⚠️  ОБЯЗАТЕЛЬНО СМЕНИТЕ ПАРОЛЬ ПОСЛЕ ПЕРВОГО ВХОДА!


═══════════════════════════════════════════════════════════════
  ПОЛЕЗНЫЕ КОМАНДЫ
═══════════════════════════════════════════════════════════════

# Статус CS2Panel
systemctl status cs2panel-hypervisor

# Логи CS2Panel
journalctl -u cs2panel-hypervisor -f

# Перезапуск CS2Panel
sudo systemctl restart cs2panel-hypervisor

# Список VM
virsh list --all

# Информация о системе
lscpu
free -h
df -h
nvme list (если есть NVMe)

# Сеть
ip addr
ip route
ping -c 4 8.8.8.8


═══════════════════════════════════════════════════════════════
  ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК
═══════════════════════════════════════════════════════════════

1. Скрипт выдает ошибку:
   - Проверьте логи: journalctl -xe
   - Запустите с debug: sudo bash -x ./install-hypervisor.sh

2. CS2Panel не запускается:
   - Проверьте логи: journalctl -u cs2panel-hypervisor -n 50
   - Проверьте daemon: ls -la /usr/local/bin/hypervisor-daemon

3. libvirt не работает:
   - Перезапустите: sudo systemctl restart libvirtd
   - Проверьте KVM: lsmod | grep kvm

4. Нет сети:
   - Переключитесь на NAT в VMware
   - Или настройте статический IP (см. выше)


═══════════════════════════════════════════════════════════════
  ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ
═══════════════════════════════════════════════════════════════

📖 Полное руководство:
   C:\Users\Галина\12345\docs\INSTALL_UBUNTU_24.md

🚀 Быстрый старт:
   C:\Users\Галина\12345\docs\QUICK_START.md

📋 Обзор:
   C:\Users\Галина\12345\UBUNTU_INSTALL_README.md

🔧 Скрипт:
   C:\Users\Галина\12345\scripts\install-hypervisor.sh


═══════════════════════════════════════════════════════════════
  УДАЧИ! 🚀
═══════════════════════════════════════════════════════════════

Если возникнут вопросы - просто напишите мне!

Я помогу с:
  ✅ Настройкой сети
  ✅ Устранением ошибок
  ✅ Компиляцией hypervisor daemon
  ✅ Созданием первой VM
  ✅ Любыми другими вопросами

Приятной работы! 😊
